version: '3.8'

services:
  mai-tools:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mai-tools
    volumes:
      - ..:/workspaces/mai-tools
    ports:
      - "8080:8080"
    working_dir: /workspaces/mai-tools
    user: node
    command: sleep infinity
